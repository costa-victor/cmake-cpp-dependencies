@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/@PROJECT_NAME@Targets.cmake")

# Make the include directories available to consumers
set_target_properties(someLibA::someLibA PROPERTIES
    INTERFACE_INCLUDE_DIRECTORIES "@someLibA_INCLUDE_DIRS@"
)

# Check whether or not all the required components were found
check_required_components(@PROJECT_NAME@)
